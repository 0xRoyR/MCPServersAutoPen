# MCP HTTP Server Configuration
# IMPORTANT: INTERNAL_API_KEY must match the value in AutoPenAgents/.env and the root .env
INTERNAL_API_KEY=change-me-use-a-strong-random-string-here
MCP_HTTP_PORT=5003

# ===================================
# Database Configuration — MySQL
# Shared with TypeScript backend (same Docker instance)
# ===================================
DB_HOST=192.168.21.128
DB_PORT=3306
DB_USER=autopen
DB_PASSWORD=autopen_secret
DB_NAME=autopen
