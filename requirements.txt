# MCPServersAutoPen HTTP tool server — Python deps (derived from imports across the repo).
# Install into the venv: .venv/bin/pip install -r requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
playwright==1.60.0

# Optional — only needed for the DB-backed recon-query tools (server runs without a DB).
pymysql==1.2.0

# ── External (non-pip) tool dependencies ─────────────────────────────────────
# pip cannot install these — they are Go/apt binaries the MCP tools shell out to.
# The one-command installer  ../setup.sh  installs them all; see ../UBUNTU_SETUP.md.
#
# run_nosqlmap  : self-contained — needs NOTHING beyond `requests` above. The Go
#                 tool below is an OPTIONAL corroborator for GET targets:
#                     go install github.com/Charlie-belmer/nosqli@latest
# run_browser   : playwright (above) + browsers →  playwright install --with-deps chromium
